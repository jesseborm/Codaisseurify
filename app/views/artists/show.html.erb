<h3> <%= @artist.name %> </h3>

<ul>
  <% @artist.songs.each do |song| %>
    <li> <%= song.title %> </li>
  <% end %>
</ul>

<br>
<hr>
<br>
<%= link_to "New Song", new_artist_song_path(@artist) %>
<br>
<%= link_to "Back", artists_path %>
